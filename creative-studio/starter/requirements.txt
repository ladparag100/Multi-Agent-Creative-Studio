# Streamlit Community Cloud installs this with pip (not uv). Versions are
# pinned exactly, including two prereleases required by google-adk==1.31.1's
# own dependency pins (opentelemetry-resourcedetector-gcp, pydantic) - pip
# installs an exact-pinned prerelease without needing a --pre flag.
google-adk[a2a]==1.31.1
opentelemetry-resourcedetector-gcp==1.12.0a0
pydantic==2.14.0a1
google-genai>=1.51.0
google-cloud-storage>=2.10.0
python-dotenv>=1.0.0
streamlit>=1.60.0
requests>=2.31.0
